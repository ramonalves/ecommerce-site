<%- contentFor('body') %>
<section id="products">
	<div class="container">
		<div class="row">
			<div class="col-xs-12 col-md-3 col-lg-3 cat">
				<h4 class="title upper bold">Categorias</h4>
				<ul class="list-group">
					<% for (var i = 0; i < categories_menu.length; i++) {%>
						<li class="list-group-item">
							<a href="/produtos/categoria/<%= categories_menu[i].slug %>"><%= categories_menu[i].name %></a>
						</li>
					<% } %>
				</ul>
			</div>
			<div class="col-xs-12 col-md-9 col-lg-9 list_pro">
				<h4 class="title upper bold">Destaques</h4>
				<div class="row">
					<% for (var i = 0; i < products.length; i++) {%>
						<div class="col-xs-12 col-md-4 product_item">
							<div class="preview">
								<img src="<%= products[i].image %>" class="img-responsive center-block"  alt="">
							</div>
							<div class="info">
								<a href="/produtos/<%= products[i].slug %>"><%= products[i].name %></a>
								<div class="price">
									<strong>R$ <%= products[i].price %></strong>
									<a href="/produtos/<%= products[i].slug %>" class="btn btn-red-default">Detalhes</a>
								</div>
							</div>
						</div>
					<% } %>														
				</div>
			</div>
		</div>
	</div>
</section>

<section id="line"></section>

<section id="steps">
	<div class="container">
		<div class="row">
			<h4 class="title upper bold">Por que a L é a melhor escolha? </h4>
			<div class="col-xs-12 col-md-4 step">
				<i class="fa fa-lock one"></i>
				<h4 class="upper bold">Site seguro</h4>
				<p>O nosso site possui os principais certificados de segurança.</p>
			</div>
			<div class="col-xs-12 col-md-4 step">
				<i class="fa fa-truck two"></i>
				<h4 class="upper bold">Entrega rápida</h4>
				<p>Entregamos em todo o Brasil de forma rápida e eficiente.</p>
			</div>
			<div class="col-xs-12 col-md-4 step">
				<i class="fa fa-heart three"></i>
				<h4 class="upper bold">Relacionamento com o cliente</h4>
				<p>A satisfação do cliente é a nossa principal preocupação.</p>
			</div>
		</div>
	</div>
</section>